# Optimize npm for Render.com builds
# Reduce unnecessary output
loglevel=error
progress=false

# Use cache efficiently
prefer-offline=true
prefer-dedupe=true

# Skip optional dependencies
optional=false

# Faster installs - skip audits and funding messages
audit=false
fund=false

# Don't update package-lock during install
package-lock=false
